<%= form_for(@user) do |f| %>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, required: :true %>
  </div>
  <div class="actions">
    <%= f.submit 'Search Flights' %>
  </div>
<% end %>
